{
  "config": {
    "step": {
      "init": {
        "title": "ICA Shopping Configuration",
        "description": "{warning_text}"
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "ICA Shopping Configuration",
        "description": "Update the session ID (access token) when it has expired, or change the list/options. {warning_text}",
        "data": {
          "session_id": "Session ID (thSessionId / access token)",
          "ica_list_id": "ICA list ID",
          "todo_entity_id": "To-do list entity",
          "remove_striked": "Remove checked and completed items"
        }
      }
    }
  },
  "issues": {
    "invalid_session_id": {
      "title": "Session ID is invalid",
      "description": "The ICA integration cannot fetch data. Open the integration and update the session ID in the configuration.",
      "severity": "error"
    }
  },
  "remove_striked_both": {
    "name": "Remove checked and completed items",
    "description": "Removes checked rows from ICA and completed items from Keep"
  }
}
